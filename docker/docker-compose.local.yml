services:
  frontend:
    image: bin05/fab-fe:1.0.0
    ports:
      - "5173:80"
    restart: always

  backend:
    image: bin05/fab-be:1.0.0
    ports:
      - "8000:8080"
    env_file:
      - ../backend/.env
    restart: always
